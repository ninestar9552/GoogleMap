<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.TranslucentStatus" parent="AppTheme">
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- START 텍스트필드 스타일 -->
    <style name="ComEditText" parent="Text2">
        <item name="android:minHeight">42dp</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingBottom">2dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">42dp</item>
        <item name="android:gravity">center_vertical|start</item>
        <item name="android:singleLine">true</item>
        <item name="android:textCursorDrawable">@color/yellow_1_ffd164</item>
        <item name="android:background">@drawable/bg_selector_com_edt</item>
        <item name="android:textColor">@color/dark_1_212529</item>
        <item name="android:textColorHint">@color/light_6_adb5bd</item>
    </style>
    <!-- END 텍스트필드 스타일 -->

</resources>